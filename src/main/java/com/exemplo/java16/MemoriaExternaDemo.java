package com.exemplo.java16;

// Requer: --add-modules jdk.incubator.foreign

//import jdk.incubator.foreign.*;

//import java.nio.charset.StandardCharsets;

//Importante: essa API foi evoluindo e substituída posteriormente pelo Panama Foreign Function & Memory API. No Java 16, o uso é mais experimental.
public class MemoriaExternaDemo {
//    public static void main(String[] args) {
//        try (MemorySegment segment = MemorySegment.allocateNative(100)) {
//            MemoryAccess.setByteAtOffset(segment, 0, (byte) 65); // escreve 'A'
//            byte b = MemoryAccess.getByteAtOffset(segment, 0);
//            System.out.println("Valor da memória: " + (char) b);
//        }
//    }
}

